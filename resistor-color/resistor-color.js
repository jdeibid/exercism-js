export const colorCode = (color) => {
  return (COLORS.indexOf(color) > -1 ? COLORS.indexOf(color) : COLORS)
};

export const COLORS = ["black", "brown", "red", "orange", "yellow", "green", "blue", "violet", "grey", "white"];
